# No required dependencies - the script uses Python stdlib only
# Web scraping for flight prices (携程/去哪儿) and 12306 train API
# work out of the box without any additional packages.

# Optional: Amadeus Python SDK (only for existing Amadeus API key holders)
# amadeus>=12.0.0

# Optional: Skyscanner library dependencies (only for SKYSCANNER_PROXY users)
# These are needed if you set SKYSCANNER_PROXY to use the Skyscanner data source.
# curl_cffi>=0.6.0      # TLS fingerprint emulation for bypassing PerimeterX
# typeguard>=3.0.0      # Type checking decorators used by skyscanner_lib
# orjson>=3.9.0         # Fast JSON parsing used by skyscanner_lib
